template <class... Args>
Status HERMES::Register(TraitSchema<Args...> &ts,
                        const std::string &name,
                        Context &ctx) {
  Status ret = 0;
      
  LOG(INFO) << "Registering Trait " << name << '\n';
      
  return ret;
}

